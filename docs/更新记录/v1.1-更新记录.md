# PDF 信息提取工具 v1.1 更新记录

## 版本信息

| 项 | 内容 |
|---|---|
| 版本号 | v1.1 |
| 基于版本 | v1.0 |
| 发布日期 | 2026-06-09 |
| 产物 | `dist/PDF信息提取工具_v1.1.exe` |

## Bug 修复（v1.0 → v1.1 期间）

| 编号 | 问题 | 修复 | 记录 |
|---|---|---|---|
| B1 | Win7 离线运行报 `DLL load failed while importing _rust` | 钉 `cryptography==3.3.2`（纯 C，无 Rust） | [bug/cryptography-rust-dll-win7.md](../bug/cryptography-rust-dll-win7.md) |
| B2 | Win7 缺少 VC++ 运行时 / UCRT DLL | 24 个运行时 DLL 打包进 EXE | 同上 |
| B3 | pip SSL 报 `check_hostname requires server_hostname` | curl 下载 wheel 本地安装 | [bug/pip-ssl-check-hostname-error.md](../bug/pip-ssl-check-hostname-error.md) |
| B4 | `--onefile` 打包后内置资源无法读取 | `get_app_dir()`/`get_exe_dir()` 路径分离 | [.claude/memory/pdf-extractor-win7-packaging-fix.md](../.claude/memory/pdf-extractor-win7-packaging-fix.md) |
| B5 | PyInstaller 打包缺少 `six` 依赖 | `requirements.txt` 补 `six==1.17.0` | — |

## 新增功能

| 编号 | 功能 | 涉及文件 |
|---|---|---|
| F1 | 一键清空全部已确认内容（「清空全部」按钮） | `main_window.py` |
| F2 | Excel 导出成功后自动清空文件列表和已确认内容 | `main_window.py` |
| F3 | 配置规则时可多次框选（同一 PDF 窗口持续复用） | `config_dialog.py` + `pdf_preview_dialog.py` |

## 全部改动文件清单

| 文件 | v1.0 → v1.1 改动 |
|---|---|
| `src/pdf_extractor/main_window.py` | F1: 新增 `btn_clear_records` + `_clear_all_records()`；F2: `_export_excel()` 点「关闭」自动清空 |
| `src/pdf_extractor/config_dialog.py` | F3: 非模态复用 `_preview_dlg`，`show()` 替代 `exec_()`，实时行绑定 |
| `src/pdf_extractor/pdf_preview_dialog.py` | F3: 「确认坐标」不再关闭窗口，自动清除框选 + 状态提示 |
| `src/pdf_extractor/requirements.txt` | B1: `cryptography==3.3.2`；B5: `six==1.17.0` |
| `src/pdf_extractor/PDF信息提取工具.spec` | B2: `binaries` 打包 24 个运行时 DLL；B4: `datas` 内嵌资源；`hiddenimports` 补齐 |
| `src/pdf_extractor/app_utils.py` | B4: 新增 `get_exe_dir()`，`get_app_dir()` 返回 `_MEIPASS` |
| `src/pdf_extractor/main.py` | B4: 样式表 `_MEIPASS` / EXE 目录双路径 |
| `src/pdf_extractor/session_manager.py` | B4: `workspace.json` 使用 `get_exe_dir()` |
| `src/pdf_extractor/config_manager.py` | B4: `get_default()` 双路径查找 |
| `src/pdf_extractor/build.bat` | B4: 改用 `.spec` 打包，Win7 部署提示 |
| `src/pdf_extractor/redist/` | B2: 24 个运行时 DLL（新增目录） |
